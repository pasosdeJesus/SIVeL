<div class='control-group nested-fields'>
  <div class="controls">
    <% if f.object.persona.nil?
          f.object.persona = Persona.new
          nil
       end %>
       
    <%= render 'contacto_fields', :f => f %>

    <%= link_to_remove_association "Eliminar Víctima", f, :class => 'btn-danger' %> 
  </div>
</div>
