<%= simple_form_for @usuario, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :id, label: "Código", readonly: true%>
  <%= f.input :nusuario %>
  <%= f.input :nombre %>
  <%= f.association :regionsjr, 
    collection: Regionsjr.where(fechadeshabilitacion: nil).order(:nombre),
    label: "Oficina",
    label_method: :nombre,
    value_method: :id,
    include_blank: true
  %>
  <%= f.input :descripcion %>
  <%= f.input :rol,
    collection: Ability::ROLES,
    label: "Rol",
    include_blank: false
  %>
  <%= f.input :idioma %>
  <%= f.input :email %>
  <%= f.input :encrypted_password %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                usuarios_path, :class => 'btn' %>
  </div>
<% end %>
